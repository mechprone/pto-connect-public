[phases.setup]
nixPkgs = ["nodejs", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "rm -rf node_modules/.cache",
  "pnpm run build"
]

[start]
cmd = "npx serve dist -s -p 3000"
